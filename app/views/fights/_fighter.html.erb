<div>
  <%= link_to @fight.new_record? ? fights_path(fighter_id: fighter.id) : fight_path(@fight, fighter_id: fighter.id), method: @fight.new_record? ? "post" : "patch" do %>
    <%= image_tag image_url(fighter.image), size: "100x100", class: "fighter-icon" %>
  <% end %>
  <div>
    LP: <%= fighter.life_points %>
    AP: <%= fighter.attack_points %>
  </div>
</div>